schema: './schema.graphql'
documents: 'apps/**/*.graphql'
generates:
  libs/apollo/src/lib/types.generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
